@inject IResourceHandler resourceHandler
@inject IUIManager uiManager

<CardFooter Border="Border.Is0" style="@($"background-color: {uiManager.Theme.BackgroundOptions.Secondary};")">
    <Container Fluid>
        <Row>

            <Column ColumnSize="ColumnSize.IsFull" TextAlignment="TextAlignment.End" Style="@($"color: {uiManager.Theme.TextColorOptions.Muted};")">
                <Text>
                    @($"{resourceHandler.GetResource(AttributeExtensions.GetResourceFieldValue<TaskMessageDto>(nameof(TaskMessageDto.DateCreated), PropertyDisplayResourceType.FIELD))}: ")
                </Text>
                <Span TextWeight="TextWeight.Bold">
                    @(Message.DateCreated.ToLocalTime().ToString("dd.MM.yyyy - HH:mm:ss"))
                </Span>
            </Column>

        </Row>
    </Container>
</CardFooter>

@code {
    [CascadingParameter]
    public TaskMessageDto Message { get; set; } = null!;
}
